/**
 * The sum calculation of two numbers.
 * @param {number} a - 1st number
 * @param {number} b - 2nd number
 * @returns {number} - sum of both a and b
 */
function sum(a, b) {
    return a + b;
  }
  export default sum;
  